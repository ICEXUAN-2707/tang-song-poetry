# 《灯下诗词》项目文档

> 一份面向「了解项目风格与架构」以及「从零学习 HTML / CSS / JS」的双目标文档。
> 仓库地址：https://github.com/ICEXUAN-2707/tang-song-poetry
> 技术栈：原生 HTML + CSS + JavaScript（经典脚本，无框架、无构建步骤）
> 在线地址：https://icexuan-2707.github.io/tang-song-poetry/

---

## 目录

- [第一部分 · 项目概览与风格定位](#第一部分-项目概览与风格定位)
- [第二部分 · 整体架构](#第二部分-整体架构)
  - [2.1 文件结构](#21-文件结构)
  - [2.2 四层职责（数据 / 状态 / 视图 / 编排）](#22-四层职责数据--状态--视图--编排)
  - [2.3 数据流](#23-数据流)
  - [2.4 经典脚本加载顺序](#24-经典脚本加载顺序)
- [第三部分 · 各文件逐层详解](#第三部分-各文件逐层详解)
  - [3.1 index.html（骨架）](#31-indexhtml骨架)
  - [3.2 CSS 四件套](#32-css-四件套)
  - [3.3 js/data.js（数据层）](#33-jsdatajs数据层)
  - [3.4 js/state.js（状态层）](#34-jsstatejs状态层)
  - [3.5 js/ui.js（视图层）](#35-jsuijs视图层)
  - [3.6 js/app.js（编排层）](#36-jsappjs编排层)
- [第四部分 · 设计风格解析](#第四部分-设计风格解析)
- [第五部分 · 从零学习 HTML / CSS / JS](#第五部分-从零学习-html--css--js)
  - [5.1 HTML 入门](#51-html-入门)
  - [5.2 CSS 入门](#52-css-入门)
  - [5.3 JavaScript 入门](#53-javascript-入门)
- [第六部分 · 运行、部署与维护](#第六部分-运行部署与维护)
- [附录 · 扩展指南与踩坑记录](#附录-扩展指南与踩坑记录)

---

# 第一部分 · 项目概览与风格定位

《灯下诗词》是一个**传统水墨国风的唐诗宋词鉴赏单页应用（SPA 思路）**。它收录了 50 首经典诗词（唐诗 25 首 + 宋词 25 首），支持卡片浏览、详情弹窗、收藏与双色主题切换。

**风格关键词：**

| 维度 | 表现 |
|------|------|
| 色彩 | 宣纸暖白 + 浅褐 + 胭脂红点缀（亮色）；深灰玄青 + 金铜点缀（暗色） |
| 字体 | 思源宋体（Noto Serif SC），营造古籍排版气质 |
| 图形 | 水墨 Hero 背景图、竖排标题、朱红印章元素、卡片留白 |
| 交互 | 卡片悬浮浮起、弹窗淡入、主题平滑过渡 |
| 模式 | 「宣纸 / 墨夜」双色主题一键切换并记忆 |
| 工程 | 纯前端、零依赖、零构建，双击 `index.html` 即可运行 |

**设计目标：**
1. **内容可赏** —— 每首诗词含原文、译文、作者介绍。
2. **浏览可寻** —— 左侧「唐诗 / 宋词 / 收藏」三模块覆盖「分类浏览 → 个人收藏」。
3. **视觉统一** —— 全局 CSS 变量驱动，改一处即全局生效。
4. **体验轻量** —— 收藏数据用 `localStorage` 持久化，刷新不丢失。

---

# 第二部分 · 整体架构

## 2.1 文件结构

```
tang-song-poetry/
├── index.html            # 页面骨架：侧边栏 + 主内容区 + 详情弹窗
├── css/
│   ├── variables.css     # 全局变量与双色主题（light / dark）
│   ├── base.css          # 重置、排版、无障碍
│   ├── layout.css        # 侧边栏 / 主内容 / 网格 / 响应式
│   └── components.css    # Hero、卡片、弹窗、收藏按钮、页脚
├── js/
│   ├── data.js           # 诗词数据 + 查询工具函数
│   ├── state.js          # 状态管理 + localStorage 持久化
│   ├── ui.js             # 所有 DOM 渲染与事件绑定
│   └── app.js            # 入口：初始化、业务编排
└── assets/
    ├── hero-tang.png     # 唐诗 Hero（山水诗）
    ├── hero-song.png     # 宋词 Hero（春江花月夜）
    └── hero-favorites.png  # 收藏 Hero（藏）
```

## 2.2 四层职责（数据 / 状态 / 视图 / 编排）

本项目刻意采用 **「数据 → 状态 → 视图 → 编排」** 的分层思路（类似 MVC / 单向数据流），四个文件**互不 import，全部由 `app.js` 协调**，从而做到真正的低耦合、高可读。

| 层 | 文件 | 职责 | 不做什么 |
|----|------|------|----------|
| 数据层 | `data.js` | 持有 `POEMS` 数组与查询函数 | 不碰 DOM、不碰状态 |
| 状态层 | `state.js` | 持有 `state` 对象、主题与收藏持久化 | 不负责渲染 |
| 视图层 | `ui.js` | 把数据画到屏幕、把用户操作转交逻辑层 | 不含业务判断 |
| 编排层 | `app.js` | 把 UI 事件与 state 变更连接起来 | 尽量不做 DOM 细节 |

> **为什么这样分？** 单一职责 + 单一数据源（Single Source of Truth）。所有 UI 不直接读写数据，而是统一经过 `state.js`，后期维护、加功能、换皮肤都更可控。

## 2.3 数据流

```
用户操作（点击导航/卡片/收藏/模式）
        │
        ▼
   app.js 中的 handler（handleViewChange / handleCardClick …）
        │
        ▼
   调用 state.js 的变更函数（setView / toggleFavorite / toggleTheme）
        │
        ▼
   state 更新 → 调用 ui.js 的 render* 函数重新渲染对应区域
        │
        ▼
   页面即时反馈（Hero 图、卡片列表、弹窗、计数同步刷新）
```

一句话：**用户操作 → 更新 state → 重新渲染对应 UI 模块 → 页面即时反馈。**

## 2.4 经典脚本加载顺序

在 `index.html` 末尾按固定顺序加载，利用「全局作用域共享」实现解耦：

```html
<script src="js/data.js?v=20260903-2210"></script>
<script src="js/state.js?v=20260903-2210"></script>
<script src="js/ui.js?v=20260903-2210"></script>
<script src="js/app.js?v=20260903-2210"></script>
```

- 顺序不能乱：`state.js` 用到 `data.js` 的 `POEMS`，`ui.js` 用到 `state` / `VIEWS`，`app.js` 用到前面全部。
- **`?v=…` 是「缓存戳」**：修改文件后改戳即可强制浏览器重新拉取，避免旧版坏脚本被缓存。
- 采用 classic script（非 ES Module），因此可直接以 `file://` 双击打开，无需起服务器。

---

# 第三部分 · 各文件逐层详解

## 3.1 index.html（骨架）

- `<aside class="sidebar">`：左侧导航，含品牌名「灯下诗词 / 白灯客」、三个 `nav-item`（唐诗 / 宋词 / 收藏）、底部「模式切换」按钮与收藏计数（`#favCount` / `#totalCount`）。
- `<main class="main">`：主内容区，含 `<header class="hero" id="hero">`（JS 注入背景图）、`<section class="poem-grid" id="poemGrid">`（卡片网格）、`<div class="empty-state">`（空状态）、`<footer class="site-footer">`（版权）。
- `<dialog class="detail-modal" id="detailModal">`：原生对话框元素，用于诗词详情弹窗。
- `<head>` 内嵌一段**全局错误浮层**脚本：`window.addEventListener("error", …)` 会在任何脚本出错时在页面顶部显示红条，避免「白屏但不知原因」。
- 字体通过 Google Fonts 引入思源宋体；全部 CSS / JS 均带缓存戳。

## 3.2 CSS 四件套

| 文件 | 关键内容 |
|------|----------|
| `variables.css` | 定义 `--font-serif`、动效时长；通过 `[data-theme="light"]` 和 `[data-theme="dark"]` 两套变量控制全局配色（背景、文字、边框、强调色、阴影、Hero 渐变）。 |
| `base.css` | `* { box-sizing: border-box }` 重置盒模型；`body` 基础排版与主题色过渡；`.skip-link` 无障碍跳转；`@media (prefers-reduced-motion: reduce)` 尊重「减少动画」系统设置。 |
| `layout.css` | `body { display:flex }` 两栏布局；`.sidebar { position:fixed; width:220px }`；`.main { margin-left:220px }`；`.poem-grid` 用 `grid-template-columns: repeat(auto-fill, minmax(280px,1fr))` 自适应列数；`@media (max-width:900px / 600px)` 响应式（侧栏转顶部横排、卡片单列）。 |
| `components.css` | `.hero`（背景图 + 底部渐隐 `::after`）、`.site-footer`、`.poem-card` 及 `.favorite-btn`、`.detail-modal`（原生 `<dialog>` 的 `[open]` 状态与 `::backdrop` 模糊遮罩）、`.btn`。 |

**双色主题原理：** 所有颜色都写成 `var(--xxx)`，切换时只需 `body[data-theme="dark"]`，浏览器自动换色。`renderTheme()` 一行 `body.setAttribute("data-theme", state.theme)` 即可全局生效。

## 3.3 js/data.js（数据层）

- `const POEMS = [ ... ]`：50 首诗词对象数组。每首字段：
  - `id`：唯一标识（`t1`~`t25` 唐诗，`s1`~`s25` 宋词）
  - `title` / `author` / `dynasty` / `category`（`"tang"` | `"song"`）
  - `content`：原文，用 `\n` 分行
  - `translation`：译文
  - `authorIntro`：作者简介
- 纯函数工具：
  - `getPoemsByCategory(category)` → 按类别筛选
  - `getPoemById(id)` → 按 ID 查单首
  - `searchPoems(keyword)` → 标题/作者/朝代/诗句全文模糊搜索（注：搜索 UI 已移除，但函数保留可作扩展）

## 3.4 js/state.js（状态层）

核心是一个状态对象 **`state`（唯一数据源）**：

```js
const state = {
    currentView: VIEWS.TANG,                  // 当前模块
    theme: loadTheme(),                       // 主题（从 localStorage 恢复）
    favorites: new Set(loadFavorites())       // 收藏集合（用 Set 去重、查询快）
};
```

关键 API：
- `loadTheme() / saveTheme()`、`loadFavorites() / saveFavorites()`：封装 `localStorage` 读写，**全部包在 try/catch 中**，在隐私模式/禁用存储时静默失败而不崩站。
- `toggleFavorite(id)` / `toggleTheme()` / `setView(view)`：变更函数，返回新值。
- `getDisplayedPoems()`：根据 `currentView` 计算「当前应显示哪些诗词」（唐诗 / 宋词 / 收藏）。
- `isFavorite(id)`：判断收藏状态。

存储键：`qixiu_poem_favorites`（收藏）、`qixiu_theme`（主题）。

## 3.5 js/ui.js（视图层）

- `elements`：在脚本（body 末尾）运行时一次性缓存所有用到的 DOM 元素，避免重复查询。
- `HERO_IMAGES`：视图 → 背景图映射（唐诗 / 宋词 / 收藏三张水墨图）。
- 渲染函数：`renderHero()`（只注入背景图、不叠文字）、`renderNavigation()`（导航高亮）、`renderTheme()`（应用主题）、`createPoemCard()` / `renderPoemGrid()`（用 `DocumentFragment` 批量插入，减少重排）、`renderDetailModal()`（详情四段：标题 → 作者 → 原文 → 作者简介）、`updateFavCount()`。
- 事件绑定（**事件委托**，把监听绑在父容器上，避免给每张卡片单独绑）：
  - `bindGridEvents`：卡片点击 / 收藏按钮点击 / 键盘 Enter+Space 打开详情。
  - `bindModalEvents`：弹窗关闭、背景点击关闭、弹窗内收藏切换。
  - `bindNavEvents` / `bindThemeEvent`：导航与主题切换。
- `openModal() / closeModal()`：调用原生 `<dialog>.showModal()`，并切换 `body` 的滚动锁。

## 3.6 js/app.js（编排层）

作为唯一入口，把「UI 事件」和「state 变更」连接起来：

- `refreshView()`：根据当前 state 重渲染 Hero、导航、卡片、计数。
- `handleViewChange / handleThemeToggle / handleCardClick / handleFavoriteToggle`：各业务 handler，只做「调用 state 函数 + 调用 ui 渲染」，**不含 DOM 细节**。
- `init()`：应用主题 → 绑定各区域事件 → 首次渲染。
- `bootstrap()`：用 `try/catch` 包裹 `init()`，初始化异常时直接在页面顶部显示错误栈，便于排查。
- 启动：`document.readyState === "loading"` 时等 `DOMContentLoaded`，否则立即 `bootstrap()`。

---

# 第四部分 · 设计风格解析

**配色哲学（在 `variables.css` 定义）：**
- 亮色「宣纸」：背景 `#f3efe6`（米黄纸感）、卡片半透 `#fffdf8`、强调 `#b5495b`（胭脂红）。
- 暗色「墨夜」：背景 `#1d1d1d`、卡片 `#262626`、强调 `#d4a373`（金铜）。

**字体：** 思源宋体，`line-height: 1.8` 拉大行距，配合竖排（`writing-mode: vertical-rl`）标题营造古籍感。

**水墨 Hero：** 三个视图各配一张水墨背景图，底部用 `linear-gradient` 渐隐自然过渡到正文，不压文字。

**双色主题切换：** 不写死颜色，全部用 CSS 变量；切换即换 `data-theme` 属性，并 `localStorage` 记忆。

**响应式断点：** ≤900px 侧栏转顶部横排；≤600px 卡片单列、导航换行。

---

# 第五部分 · 从零学习 HTML / CSS / JS

> 本部分面向「零基础、从 0 开始」的团队成员。我们会结合本项目真实代码片段讲解，学完即可在本项目上做改动与扩展。

## 5.1 HTML 入门

**HTML 是什么？** 一套用「标签」描述页面结构的标记语言。标签通常成对出现：`<标签名>内容</标签名>`。

**本项目里你能看到的标签：**

```html
<!-- 标题与段落 -->
<h1 class="sidebar__title">灯下诗词</h1>
<p class="sidebar__subtitle">白灯客</p>

<!-- 容器：div 无语义，section/aside/main/footer 有语义（对无障碍友好） -->
<aside class="sidebar"> ... </aside>
<main class="main"> ... </main>
<footer class="site-footer"> ... </footer>

<!-- 按钮：可点击交互 -->
<button class="nav-item" data-view="tang">唐诗</button>

<!-- 对话框：原生弹窗元素 -->
<dialog class="detail-modal" id="detailModal"> ... </dialog>

<!-- 引入外部资源 -->
<link rel="stylesheet" href="css/variables.css">
<script src="js/app.js"></script>
```

**三个关键概念：**
1. **属性（attribute）**：写在标签里，如 `class="xxx"`（样式钩子）、`id="xxx"`（JS 精确定位）、`href/src`（资源路径）、`data-view="tang"`（自定义数据，JS 用 `dataset.view` 读取）。
2. **class vs id**：class 可重复使用（多张卡片），id 全页唯一（如 `#poemGrid`）。
3. **语义化标签**：`nav`/`main`/`footer` 让屏幕阅读器和搜索引擎更好理解结构。

**动手练习：** 在 `index.html` 的 `.sidebar__menu` 里复制一个 `<button class="nav-item" data-view="tang">`，把文字改成「乐府」，刷新页面看效果（暂不会切换，后面接状态即可）。

## 5.2 CSS 入门

**CSS 是什么？** 一套「选择器 { 属性: 值 }」的规则，决定元素长什么样。

```css
/* 选择器：选到所有 .poem-card */
.poem-card {
    padding: 1.75rem;            /* 内边距 */
    background: var(--bg-card);   /* 背景色，引用变量 */
    border-radius: 2px;          /* 圆角 */
    box-shadow: var(--shadow-card);
    transition: transform .35s;  /* 过渡动画 */
}
.poem-card:hover {               /* 伪类：鼠标悬浮时 */
    transform: translateY(-4px); /* 上移 4px，做出浮起效果 */
}
```

**本项目用到的核心 CSS 能力：**

| 能力 | 例子 | 作用 |
|------|------|------|
| CSS 变量 | `var(--accent-color)` | 一处定义、全局复用，主题切换的关键 |
| Flexbox | `display:flex; justify-content:space-between` | 一维布局（侧栏、卡片底部） |
| Grid | `grid-template-columns: repeat(auto-fill, minmax(280px,1fr))` | 二维自适应卡片网格 |
| 媒体查询 | `@media (max-width:900px){...}` | 响应式，不同屏幕不同样式 |
| 伪元素 | `.hero::after { content:"" }` | 不写 HTML 也能加装饰层（如渐隐遮罩） |
| 伪类 | `:hover` `:focus` `[open]` | 交互/状态样式 |
| 过渡 | `transition: background-color .35s` | 让颜色/位移平滑变化 |

**双色主题实战：** 所有颜色写成 `var(--bg-body)`。亮色在 `[data-theme="light"]` 定义 `--bg-body:#f3efe6`，暗色在 `[data-theme="dark"]` 定义为 `#1d1d1d`。JS 只改 `data-theme` 属性，整站换肤。

**动手练习：** 打开 `variables.css`，把 `[data-theme="light"]` 的 `--accent-color` 从 `#b5495b` 改成 `#2e7d6b`（墨绿），刷新看强调色变化——这就是「改一处、全局生效」。

## 5.3 JavaScript 入门

**JS 是什么？** 让网页「动起来」的编程语言：处理点击、改内容、存数据。本项目用**经典脚本**（非模块），变量和函数放在全局，按顺序加载。

**① 变量与常量**
```js
const POEMS = [ /* 不变量：诗词数据 */ ];
let count = 0;          // 可变的用 let
var x = 1;              // var 较老，本项目 ui.js 用其缓存 DOM
```

**② 对象与数组**
```js
const poem = { id:"t1", title:"静夜思", author:"李白" };
POEMS[0];                       // 取数组第 1 个
poem.title;                     // 取对象属性（点语法）
```

**③ 数组方法（本项目高频）**
```js
POEMS.filter(p => p.category === "tang");   // 筛选唐诗
POEMS.find(p => p.id === "t1");             // 找 id=t1 的那首
poems.forEach(p => render(p));              // 遍历渲染
[...state.favorites]                       // 展开 Set 成数组（便于 JSON 存储）
```

**④ 函数（含回调）**
```js
function toggleTheme() { return state.theme === "dark" ? "light" : "dark"; }
// 回调：把函数作为参数传给别人，在事件发生时被调用
bindNavEvents(function(view){ handleViewChange(view); });
```

**⑤ DOM 操作（JS 与页面的桥梁）**
```js
document.getElementById("hero");            // 按 id 取元素
document.querySelector(".poem-card");       // 按 CSS 选择器取第一个
el.innerHTML = "<h3>标题</h3>";            // 写入 HTML
el.dataset.id;                              // 读 data-id 属性
el.classList.toggle("is-active", true);     // 增删 class
el.addEventListener("click", fn);           // 绑定点击事件
```

**⑥ localStorage（本地持久化）**
```js
localStorage.setItem("qixiu_theme", "dark");   // 写
const t = localStorage.getItem("qixiu_theme"); // 读
```
收藏与主题就靠它跨刷新保存，且包在 try/catch 里防止隐私模式报错。

**⑦ 事件委托（本项目关键技巧）**
不为每张卡片单独绑监听，而是绑在父容器 `#poemGrid` 上，靠 `event.target.closest(".poem-card")` 判断点到哪张：
```js
poemGrid.addEventListener("click", function(event){
    const card = event.target.closest(".poem-card");
    if (card) onCardClick(card.dataset.id);
});
```
好处：动态新增的卡片无需重新绑定，性能更好。

**⑧ 原生 `<dialog>` 弹窗**
```js
dialog.showModal();   // 打开（带遮罩、ESC 可关）
dialog.close();       // 关闭
```

**⑨ 单向数据流（本项目架构灵魂）**
永远走「用户操作 → 改 state → render 重渲染」，别在 UI 里直接偷偷改数据。这样状态可预测、好调试。

**动手练习（进阶）：** 在 `state.js` 的 `VIEWS` 里加 `LOVE: "love"`，在 `index.html` 加对应 `nav-item`，在 `ui.js` 的 `HERO_IMAGES` 登记图片，在 `getDisplayedPoems()` 增加分支——你就新增了一个完整模块。

---

# 第六部分 · 运行、部署与维护

## 本地运行
- **最简单：** 双击 `index.html`（因采用 classic script，`file://` 直接可用）。
- **推荐（更接近线上）：** 在项目目录执行 `python -m http.server 8080`，浏览器访问 `http://localhost:8080`。

## 部署
- 本项目已部署至 **GitHub Pages：https://icexuan-2707.github.io/tang-song-poetry/**（同时保留 CloudStudio 镜像：https://3fde875c9a9e4358a025253c5c7c12d3.app.workbuddy.link）
- 纯静态站点，任何静态托管（CloudStudio / GitHub Pages / Nginx）均可，无需后端。
- 发布前务必更新所有 `?v=…` 缓存戳，确保用户拿到最新文件。

## 维护注意
- 改完 JS/CSS 后同步更新 `index.html` 中对应的 `?v=` 戳。
- 新增诗词只需在 `data.js` 的 `POEMS` 追加对象，其余逻辑自动适配。
- 顶部若出现红色错误条，按提示定位脚本问题（多半是语法错误）。

---

# 附录 · 扩展指南与踩坑记录

## 扩展指南
1. **加诗词**：`data.js` 追加对象即可。
2. **加模块**：`index.html` 加 `nav-item` → `state.js` 的 `VIEWS` 登记 → `ui.js` 的 `HERO_IMAGES` 登记 → `getDisplayedPoems()` 加分支。
3. **换肤**：`variables.css` 加第三套变量值，扩展 `state.js` 的 `THEMES`。
4. **接后端**：把 `state.js` 的 `localStorage` 读写替换为 `fetch` 调用即可平滑升级。

## 踩坑记录（供团队避雷）
- **块注释陷阱**：在 `app.js` 早期版本中，注释里写了 `render*/bind*` 这样的字符，`*/` 被解析为「块注释结束符」，导致后续代码被吞、整段脚本报错、全站失灵。结论：**注释里不要出现 `*/`**；本项目已全面改用 `//` 或规范 `/* */`。
- **缓存导致「初始化失败」**：浏览器缓存了旧的坏脚本，改代码后页面仍报错。解决：加错误浮层 + `bootstrap()` try/catch + 升缓存戳。
- **经典脚本 vs 模块**：本项目刻意不用 ES Module（`type="module"`），以保 `file://` 直开可用；变量/函数置于全局，靠加载顺序保证依赖。

---

> 文档完。建议团队先通读「第五部分」打基础，再回到「第二、三部分」对照真实代码理解架构，最后用「动手练习」上手机改。
